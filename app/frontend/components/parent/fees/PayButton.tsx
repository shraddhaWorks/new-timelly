"use client";

/**
 * Re-export the app PayButton (HyperPG) so imports from ./PayButton work.
 * The real implementation is in @/components/PayButton.
 */
import PayButton from "@/components/PayButton";
export default PayButton;
