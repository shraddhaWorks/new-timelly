-- This migration was originally adding a `certificateType` column
-- to `TransferCertificate`, but the model no longer uses it.
-- Keep this file as a no-op so Prisma migration history stays consistent.

